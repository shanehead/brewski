type: object
required:
  - id
  - recipe_id
  - addition
  - target
  - amount
properties:
  id:
    type: string
  recipe_id:
    type: string
  addition:
    type: string
    enum:
      - gypsum
      - calcium_chloride
      - epsom_salt
      - table_salt
      - baking_soda
      - chalk
      - lactic_acid
      - phosphoric_acid
  target:
    type: string
    enum:
      - mash
      - sparge
  amount:
    type: number
    description: Amount in grams for salts, ml for acids
