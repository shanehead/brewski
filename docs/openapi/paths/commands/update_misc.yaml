post:
  operationId: updateMisc
  summary: Update a custom misc ingredient in the ingredient library
  tags:
    - Library
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - id
            - input
          properties:
            id:
              type: string
            input:
              $ref: "../../components/schemas/UpdateMiscInput.yaml"
  responses:
    "200":
      description: Updated misc ingredient
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/Misc.yaml"
    "500":
      $ref: "../../components/responses/Error.yaml"
