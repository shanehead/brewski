post:
  operationId: deleteYeast
  summary: Delete a custom yeast from the ingredient library
  tags:
    - Library
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - id
          properties:
            id:
              type: string
  responses:
    "200":
      description: Deleted
    "500":
      $ref: "../../components/responses/Error.yaml"
