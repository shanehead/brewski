post:
  operationId: updateWater
  summary: Update a custom water profile in the ingredient library
  tags:
    - Library
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - id
            - input
          properties:
            id:
              type: string
            input:
              $ref: "../../components/schemas/UpdateWaterInput.yaml"
  responses:
    "200":
      description: Updated water profile
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/Water.yaml"
    "500":
      $ref: "../../components/responses/Error.yaml"
