post:
  operationId: listWaterLibrary
  summary: List all water profiles in the library
  tags:
    - Library
  responses:
    "200":
      description: Array of water profiles
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas/Water.yaml"
    "500":
      $ref: "../../components/responses/Error.yaml"
